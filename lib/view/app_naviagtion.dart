import 'package:flutter/material.dart';

class AppNaviagtion extends StatelessWidget {
  const AppNaviagtion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}